# Env example (secrets go in real .env — never commit)

# LLM
LLM_PROVIDER=
LLM_MODEL=
LLM_API_KEY=

# Database
DATABASE_URL=postgresql://user:pass@localhost:5432/investing_insight

# Market / filings providers (names TBD at L00+)
MARKET_DATA_API_KEY=
# SEC: often keyless; user-agent required by policy — set when implementing

# App
NODE_ENV=development
API_BASE_URL=http://127.0.0.1:8000
